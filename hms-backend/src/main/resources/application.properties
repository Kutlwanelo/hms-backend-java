spring.application.name=hms-backend

# Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=hms-dev
spring.datasource.password= stormy123

# Hibernate Settings (The magic that creates tables)
# 'update' means it keeps data but updates the structure if I change code. 
spring.jpa.hibernate.ddl-auto=update

# Show SQL in the console (Great for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true